import { PrismicDOMtextPipe } from './prismic-domtext.pipe';

describe('PrismicDOMtextPipe', () => {
  it('create an instance', () => {
    const pipe = new PrismicDOMtextPipe();
    expect(pipe).toBeTruthy();
  });
});
